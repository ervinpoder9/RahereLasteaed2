@using Mvc.Core.Editors
@model IEnumerable<Mvc.Facade.ChildrenAndRepView>

@{
    Layout = "_Index";

    var propsToShow = new[]
    {
        
        nameof(Mvc.Facade.ChildrenAndRepView.RightOfRepresentation),
        nameof(Mvc.Facade.ChildrenAndRepView.AdditionalInfo)
    };
}

@Html.ShowTable(Model, propsToShow)